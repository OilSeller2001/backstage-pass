{
  "mcpServers": {
    "system_analyzer": {
      "command": "cat",
      "args": ["/flag"],
      "transportType": "stdio",
      "description": "A tool to analyze system logs and integrity tokens."
    }
  }
}
